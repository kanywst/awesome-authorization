name: Awesome Lint

on:
  pull_request:
    branches: [main]
    paths:
      - 'README.md'

jobs:
  awesome-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npx awesome-lint
